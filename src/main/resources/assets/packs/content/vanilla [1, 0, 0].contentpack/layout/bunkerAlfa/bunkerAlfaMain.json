{
    "metadata": {
        "identifier": "TranscendRuins:bunkerAlfaMain"
    },
    "attributes": {
        "size": [400, 400],

        "definitions": {
            "layouts": {},
            "elements": {
                "entranceStairwell": {
                    "identifier": "TranscendRuins:stairwell",
                    "properties": {
                        "passagewayMode": "static", // Select an existing location. Required for functionality.
                        "passagewayLocation": "bunkerAlfa", // Designate the location. Default: current location. Required for functionality.
                        "passagewayArea": "floor1", // Designate the area. Default: the current area if location is not specified, otherwise the default area of the specified location.
                        "passagewayTarget": "exitStairwell", // Designate the target spawn coordinate. Default: the default spawn location of the area.
                        "linkPassagewayTarget": false // Links the return element to point to this one. Default: false IF STATIC.
                    }
                },
                "basementStairwell": {
                    "identifier": "TranscendRuins:stairwell",
                    "properties": {
                        "passagewayMode": "generative", // Create a new location. Required for functionality.
                        "passagewayLocation": "TranscendRuins:basement", // Designate the location. Required for functionality.
                        "passagewayArea": "main", // Designate the area. Default: the default area of the specified location.
                        "passagewayTarget": "exitStairwell", // Designate the target spawn coordinate. Default: the default spawn location of the area.
                        "linkPassagewayTarget": true // Links the return element to point to this one. Default: true IF GENERATIVE.
                    }
                }
            },
            "entities": {}
        },

        "generation": "blueprint",
        "components": {}
    }
}